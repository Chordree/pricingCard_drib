function Clicker({name, num=555, children}) {
    // const {name, num=555,children} = params

    return(
        <button>
             {/* {children}
            {name}
            
            {num} */}
            {children}
           
        </button>
    )
}

export default Clicker